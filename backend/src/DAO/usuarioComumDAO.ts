/*
import { UsuarioComum, UsuarioComumInput } from '../models/usuarioComumModel';

export class UsuarioComumDAO {
    // Método temporário para evitar erros de compilação
    async insertUsuario(usuario: UsuarioComumInput): Promise<any> {
        console.log('DAO: Método insertUsuario chamado com:', usuario);
        // TODO: Implementar lógica real de inserção no banco de dados
        return { success: true, message: 'Usuário inserido no banco (simulado)' };
    }

    // Método temporário para evitar erros de compilação
    async selectAllUsuarios(): Promise<UsuarioComum[]> {
        console.log('DAO: Método selectAllUsuarios chamado');
        // TODO: Implementar lógica real de busca no banco de dados
        return [];
    }
}
*/
